# Assets

This folder is reserved for portfolio images, case study thumbnails, resumes, and project media.

The current site uses a lightweight JavaScript canvas visual in the hero section, so it runs without external image dependencies.
